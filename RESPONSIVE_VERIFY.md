# Responsive Verification Notes

## Dashboard (done)
- `components/dashboard/dashboard-layout-client.tsx`
  - Desktop sidebar: `md+` visible.
  - Mobile: drawer slide-in.
  - Main uses `min-w-0` and `min-h-0`.

## Marketing/Layout (in progress)
- `app/(marketing)/layout.tsx`
  - Fixed `id="home"` sizing from `h-200` to `h-full`.

## Next pages (planned)
1. Dashboard views
2. Landing page responsiveness across required breakpoints
3. Meeting pages
4. AI Interview
5. External Chat
6. Reports
7. Settings

